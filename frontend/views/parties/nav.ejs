<nav class="bg-[#161b22] border-b border-gray-700 px-2 py-5 
            flex items-center justify-between 
            rounded-lg shadow-lg">

    <!-- LEFT: Terminal Title -->
    <div class="flex items-center space-x-2 text-lg font-semibold tech-font">
        <span class="text-green-500 font-bold">root@AaaS</span>
        <span class="text-blue-500">:~$</span>
        <span class="text-green-400">
            <%= "./" + title %>
        </span>
    </div>

    <!-- RIGHT: Login / Logout -->
    <div class="flex items-center space-x-5 text-lg tech-font">

        <% if (isLoggedIn) { %>
            <a href="/logout" class="text-red-400 hover:text-red-300 transition">
                ./logout
            </a>
            <% } else { %>
                <a href="/login" class="text-green-400 hover:text-green-200 transition">
                    ./login
                </a>

                <a href="/signup" class="text-blue-400 hover:text-blue-200 transition">
                    ./signup
                </a>
                <% } %>

    </div>

</nav>